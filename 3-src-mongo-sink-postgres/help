docker compose exec postgres bash

psql -d inventorydb -U postgresuser -W

- check postgres version
	SELECT VERSION();

- list db:
	\l


- Show tables in PostgreSQL
	CI:
		\c database_name
		\dt

	CII:
		SELECT * FROM pg_catalog.pg_tables;